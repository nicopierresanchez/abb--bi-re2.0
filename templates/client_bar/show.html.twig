{% extends 'base.html.twig' %}

{% block title %}Bar
{% endblock %}

{% block body %}
	<h1>Bar</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ bar.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ bar.name }}</td>
			</tr>
			<tr>
				<th>Type</th>
				<td>{{ bar.type }}</td>
			</tr>
			<tr>
				<th>Address</th>
				<td>{{ bar.address }}</td>
			</tr>
			<tr>
				<th>Phone</th>
				<td>{{ bar.phone }}</td>
			</tr>
			<tr>
				<th>Slug</th>
				<td>{{ bar.slug }}</td>
			</tr>
			<tr>
				<th>Image</th>
				<td>{{ bar.image }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('client_bar_index') }}">back to list</a>

	<a href="{{ path('client_bar_edit', {'id': bar.id}) }}">edit</a>

	{{ include('client_bar/_delete_form.html.twig') }}
{% endblock %}
