{% extends 'base.html.twig' %}

{% block title %}Bar
{% endblock %}

{% block body %}
	{% if app.user.id is same as bar.user.id %}

		<h1>Bar</h1>

		<table class="table table-dark table-striped">
			<tbody>
				<tr>
					<th>Name</th>
					<td>{{ bar.name }}</td>
				</tr>
				<tr>
					<th>Type</th>
					<td>{{ bar.type }}</td>
				</tr>
				<tr>
					<th>Address</th>
					<td>{{ bar.address }}</td>
				</tr>
				<tr>
					<th>Phone</th>
					<td>{{ bar.phone }}</td>
				</tr>
				<tr>
					<th>Image</th>
					<td>{{ bar.image }}</td>
				</tr>
			</tbody>
		</table>
<div class="action">
		<a href="{{ path('client_bar_index',{'id': app.user.id}) }}">Retour</a>

		<a href="{{ path('client_bar_edit', {'id': bar.id}) }}">Modifier</a>
</div>
		{{ include('client_bar/_delete_form.html.twig') }}
		{% else %}
		houlà! il semblerait que tu sois perdu !
	{% endif %}
{% endblock %}
